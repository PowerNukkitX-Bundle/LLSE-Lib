
export class NBT {
	/**
	 * 从 SNBT 字符串生成 NBT 标签对象
	 * @param snbt {string} 你要解析的 SNBT 字符串
	 * @returns {NbtCompound|null} 返回NbtCompound
	 */
	static parseSNBT(snbt) {
		//
	}
	/**
	 * 从二进制 NBT 数据生成 NBT 标签对象
	 * @param nbt {ByteBuffer} 你要解析的二进制 NBT 数据
	 * @returns {NbtCompound|null} 返回NbtCompound
	 */
	static parseBinaryNBT(nbt) {
		//
	}
}